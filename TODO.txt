Go through the Components and Make sure there is no problem with Mesh Render Component and establish a relation between component and entity so the component can access the entity through entity variable but make it immutable.